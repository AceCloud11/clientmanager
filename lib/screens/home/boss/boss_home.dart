import 'package:flutter/material.dart';

class BossHome extends StatelessWidget {
  const BossHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('boss home'));
  }
}
